# MCP Configuration

This directory contains MCP setup examples for ReCSS development.

Files:
- claude_desktop_config.example.json: full MCP server setup sample

How to use:
1. Copy the JSON content into your Claude desktop MCP config file.
2. Replace token placeholders.
3. Keep filesystem scope limited to this repository path.
